---
section: notes
reusable: true
applies-to: [how-to, reference]
---
<!-- SECTION notes — caveats / gotchas. Optional. Each: a one-line caveat + a link out
     for detail. Defer per-host / per-component specifics to the reference page rather
     than inlining them (see the `command-howto-clarity` voice lesson). -->

## Notes

- <Caveat in one line> — see [<reference page>](<link>).
