---
section: how-it-works
reusable: true
applies-to: [component-overview]
---
<!-- SECTION how-it-works — the mechanism, in plain present-tense prose. This is the
     LEAD of an architecture component overview: explain how the component actually
     works before linking anywhere. One short paragraph for the shape, then a table
     or bullets for the parallel parts (the pipeline stages, the primitive kinds, the
     per-host destinations). Tables beat prose for parallel items. State the end state
     as current — no version history, no "moved in vX" asides (that's the CHANGELOG /
     ADR's job). Field-level detail belongs to the Reference page; name it in See also,
     don't inline it here. -->

## How it works

<One paragraph: the mechanism in plain present-tense — what moves through this component and what it produces.>

| <parallel part> | <what it does> |
|---|---|
| **<…>** | <…> |
