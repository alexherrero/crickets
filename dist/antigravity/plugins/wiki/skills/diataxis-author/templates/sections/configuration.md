---
section: configuration
reusable: true
applies-to: [plugin-reference]
---
<!-- SECTION configuration — closes the ## Reference half. State what the operator sets up
     front, or "No configuration — the plugin works out of the box." When behavior is
     driven by files or env the operator touches, name them plainly (trigger files, an env
     var); and where the state lives (repo-local under .harness/ vs the synced vault) when
     that varies by project, rather than hardcoding one location. -->

### Configuration

<"No configuration — works out of the box", or the settings / files / env the operator sets.>
