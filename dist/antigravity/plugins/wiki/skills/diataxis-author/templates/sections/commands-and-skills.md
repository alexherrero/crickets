---
section: commands-and-skills
reusable: true
applies-to: [plugin-reference]
---
<!-- SECTION commands-and-skills — the lead of the ## Reference half. The plugin's
     primitives in one table. Each primitive NAME links to the source that implements it
     (src/<plugin>/…) so the page is a jump-off to the code. Keep the "What it does" cell
     to one plain line; the deep behavior lives in the source + the How it works prose. -->

### Commands & skills

Each primitive links to the source that implements it.

| Primitive | Kind | What it does |
|---|---|---|
| [`<name>`](<src-url>) | command / skill / hook / agent / snippet | <one plain line>. |
