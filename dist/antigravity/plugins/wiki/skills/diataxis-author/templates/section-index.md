---
page-template: section-index
sections:
  - intro
  - section-contents
  - recent-changes
  - see-also
---
<!--
  PAGE TEMPLATE: section-index — the landing for one wiki section (one per
  intent-folder: get-started/, do/, reference/, why/, designs/, decisions/). A
  COLLECTION OF SECTIONS; the `sections:` list IS the page.

  A section index is NOT a redirect to the first sub-page — it's a real landing:
  what the section IS, its pages as curated one-liners (not regurgitated), and a
  tooling-maintained Recent changes block. The sidebar's section heading links
  HERE, never to a sub-page. Marked `<!-- mode: index -->` — a shape-exempt landing,
  not one of the four Diátaxis modes.

  Cross-cutting opinions:
    - intro: what the section is, in a sentence or two (no marketing)
    - section-contents: curated one-liners that ORIENT, never regurgitate the pages
    - recent-changes: tooling-maintained; the returning reader's "what moved"
    - plain, present-tense prose (the `user-facing-prose` voice lesson)

  FOLLOW-UP (not built yet): the composer assembles this manifest, generates the
  Recent changes block (from git / page metadata), and emits the per-folder
  _Sidebar.md whose section heading points here.
-->
