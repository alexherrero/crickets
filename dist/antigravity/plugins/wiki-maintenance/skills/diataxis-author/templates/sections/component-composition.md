---
section: component-composition
reusable: true
applies-to: [component-overview]
---
<!-- SECTION component-composition — how THIS architecture component fits with the
     other components in architecture.yml. The architecture-level analog of
     plugin-composition: name the sibling components it actually touches and what
     crosses the seam, not a generic "it integrates with everything". One bullet per
     sibling it really depends on or feeds; link the sibling's overview. A component
     that folds INTO this one (rather than standing apart) is worth stating as such. -->

## How it fits

- **[<Sibling component>](<Overview>)** — <what crosses the seam between them>.
