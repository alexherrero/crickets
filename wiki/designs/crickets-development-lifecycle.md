---
title: development-lifecycle — design
status: launched
kind: design
scope: feature
area: crickets/development-lifecycle
governs: [src/development-lifecycle/]
parent: crickets-hld.md
seeded: 2026-06-20
approved: 2026-06-22
---

> [!NOTE]
> **LAUNCHED (lifted 2026-06-24, AG Phase 3; originally approved 2026-06-22) · locked 2026-06-28 (final AG design sweep).** child-design — the **development-lifecycle** capability: the loop a developer runs every day, a feature's whole life from plan to retirement. The base every other capability builds on. `status: launched` (lifted into tracked `wiki/designs/` 2026-06-24, AG Phase 3). Points *up* at the [crickets HLD](crickets-hld.md).

# development-lifecycle

## Objective

The development lifecycle is a developer's **bread and butter** — the loop you actually run every day, not a once-in-a-while ritual. You sit down at a repo, turn a brief into a plan, work the plan task by task, have the work torn at adversarially, and ship it under a recoverability gate. For long-lived code, you then launch it to real users, deprecate what it replaces, and retire the dead interface. Each step is a short, isolated session with one job, and the next session picks up from **durable artifacts on disk** rather than a remembered conversation. This capability names those daily cycles, pins down where each begins and ends, and makes explicit how state survives between them — whether or not the agentm memory engine is mounted underneath. It is the base every other capability builds on.

## Overview

The lifecycle, first to last:

| Step | What it does |
|---|---|
| **`/setup`** *(once)* | First-time scaffold — writes the `.harness/` state files and fills `init.sh` / `AGENTS.md` with real project commands. |
| **`/plan`** | Turns a brief into `PLAN.md` with a task list + per-task verification criteria; writes no code; grounds against the governing design when one exists. |
| **`/work`** | Works the plan's tasks autonomously in sequence (implement → gates → commit per task); stops only on a failed safety pre-check or a needed clarification. |
| **`/review`** | Runs the deterministic gates first, then an adversarial pass primed to assume bugs exist; reports a failing test or a `file:line` defect, never fixing. |
| **`/release`** | The pre-merge gate — plan done + gates green, wait for CI, then push / tag / ship under the recoverability gate; archives the completed plan. |
| **`/launch`** | The pre-launch readiness gate before first production exposure — observability, a tested rollback, a flag off-switch, a staged-rollout plan. |
| **`/deprecate`** | Marks an interface for removal — compulsory vs advisory, enumerate callers, migrate now or attach a notice + a published removal date. |
| **`/retire`** | The code-removal gate — the Beyoncé-rule audit (remove, run the suite, prove nothing breaks), then delete the proven-dead interface. |

The one branch off this road is **`/bugfix`** — for defects it replaces `/plan` + `/work` with Report → Analyze → Fix → Verify (mandatory regression test, mandatory `/review`), then rejoins the `/release` path.

![The development-lifecycle loop: /setup (once) → /plan → /work → /review → /release → /launch → /deprecate → /retire, with /bugfix as the defect track that branches off /plan and rejoins at /release — the loop a developer runs every day, a feature's whole life from plan to retirement](diagrams/crickets-development-lifecycle.svg)

*The daily loop and the feature's endpoints, first to retirement; `/bugfix` is the alternate track to plan + work; each step is a short isolated session, state carried on disk between them.*

## Design

### Development Lifecycle

This is the loop itself: short, isolated sessions chained `/plan` → `/work` → `/review` → `/release` → `/launch` → `/deprecate` → `/retire` (after a one-time `/setup`), with `/bugfix` the alternate track to `/plan` + `/work`. Four properties hold across every cycle:

- **Deterministic where it can be.** Every cycle leans on helper scripts instead of re-deriving state in-prompt — `resolve_plan.py` (plan paths + mode), `isolation_config.py` (isolation mode), `worktree_marker.py` / `finalize_unit.py` (worktree lifecycle), `worktree_shepherd.py` (periodic orphan + stalled-PR reclamation), `preflight_reconcile.py`, and the `check-all.sh` gate battery. The agent supplies judgment; the scripts do the bookkeeping, so there are fewer places for drift to creep in.
- **Gates before judgment.** The deterministic gates (typecheck → lint → tests → build) run first and short-circuit on failure, before any LLM critique — cheap, truthful checks gate the expensive ones.
- **Recoverable by default.** A recoverable push / tag / release announces itself and proceeds; only a genuinely unrecoverable action stops for confirmation (the recoverability-gate doctrine, shared with [developer-safety](crickets-developer-safety.md)).
- **Runnable one-off, in a loop, or as a goal.** Each cycle is a stateless command driven by on-disk state, so it can run as a single session, **looped over a set** (e.g. break five designs into sub-plans in one pass), or handed a **goal** to pursue until done. These are the persona launch modes (see [Personas](https://github.com/alexherrero/agentm/wiki/agentm-personas)); a looped or goal-driven run is just N isolated sessions sharing the same durable substrate.

**Durable memory, with or without agentm.** State lives on disk, never in the conversation — the next session reads the artifacts, not your context. Two layers:

- **Standalone (always).** The `.harness/` substrate works against plain files with no memory engine mounted: `PLAN.md` / `PLAN-<slug>.md`, `progress.md` (append-only, one line per task/phase, never edited), `features.json` (`passes: true|false`, set only by `/review` + `/release`), `init.sh`, `project.json` (isolation + integration config), `board-items.json` (the board source of truth), `queued-plans/` (the staging tier), and a worktree-local `active-plan` marker. Every spec writes plain `.harness/<file>` state and works standalone; the memory layer is optional and graceful-skips.
- **agentm-backed (when configured).** The plugin never hardcodes a path: it resolves through agentm's storage seam (`resolve_plan.py` delegates to the seam when present, falls back to `.harness/` otherwise). With a vault backend the same artifacts land at **`<vault>/projects/<slug>/_harness/…`**, and reflection / recall memory lands in the vault's `_harness/`. The bytes and relative layout are identical across the device-local and vault backends — the developer's day looks the same either way.

So the per-workflow **Artifacts** lines below name paths relative to that root: `.harness/<file>` standalone, or `<vault>/projects/<slug>/_harness/<file>` when agentm is mounted. Reflection / recall memory lands in the vault only; the board mirror is `board-items.json` (canonical; the GitHub Project is the human mirror); all `.harness/` is gitignored, so state is never committed.

**Session bootstrap (Claude-only).** Two hooks on the `development-lifecycle` plugin automate session setup on Claude Code:

- **`harness-context-session-start`** (`SessionStart`) — surfaces `.harness/PLAN.md` + `progress.md` at session boot so the agent reads the plan before the operator's first message. No-op when no plan is active.
- **`compact-nudge-resume`** (`UserPromptSubmit`) — on every user prompt, injects a `/clear`-over-`/compact` nudge when context ≥ 60% full or > 400 assistant turns; silent below threshold or on a fresh session. Keeps the session context lean so the state-on-disk substrate can resume cleanly.

Both hooks are Claude-only; they do not fire on Antigravity (see [Antigravity limitations](Antigravity-Limitations)).

### Routed dispatch + mandatory fan-out announcement (as-built)

The loop's sub-agent dispatch points — `/plan`'s explorer, `/review`'s adversarial + cross-model reviewers, the documenter, verification-clerk-style graders — today spawn on hardcoded agent-def frontmatter (the as-built *phase-aware-model-routing*: Opus for `/work`/`/bugfix`, Sonnet elsewhere). This adds a routed-by-default layer on top, realizing the operator's efficiency protocol (proven manually in the Mythos readiness run) as capability rather than hand-discipline:

- **Classify, then look up.** Each dispatch point first resolves its own work-type via token-audit's `classify_work_type` classifier — a persona's already-declared tier, an ad-hoc role-name match, or an explicit `UNCLASSIFIED-DEFAULT` when neither applies — then consults [token-audit](crickets-token-audit.md)'s versioned routing table (the single home of current model ids) for that work-type's `(tier, model, effort)`, via a new `enhances:` edge from token-audit + a `find_capability` probe, and passes the resolved **model** per invocation. The Agent tool exposes no per-invocation `effort`, so the effort axis keeps binding statically via agent-def `effort:` frontmatter roster-wide, unchanged by this amendment. **Graceful degradation** to today's hardcoded behavior when the table or the capability is absent — the existing `find_capability.py` pattern, unchanged.
- **Mandatory fan-out announcement, unconditional.** Before *any* sub-agent launch — capability present or absent — the dispatching agent prints one line per dispatch group: role · agent count · model (+ effort where bound) · **tier source** (table row / agent frontmatter / `UNCLASSIFIED-DEFAULT` / `INHERITED`). This is unconditional: it degrades to announcing frontmatter/inherited sources when the table isn't installed, so visibility never depends on the efficiency capability being present. The `INHERITED` source must explicitly cover **built-in-agent inheritance** alongside custom agent-defs: Explore inherits the session model starting at Claude Code 2.1.198, so once the installed CLI crosses that version the announcement has to name Explore's dispatches correctly instead of assuming the cheap-floor default still holds. The `UNCLASSIFIED-DEFAULT` source names the classifier's own miss case — a role the classifier couldn't match, running on the fixed safe default — so a classification gap is visible traffic, not silent success.
- **Silent session-model inheritance is forbidden.** Every dispatch sets its model explicitly, from the table, from frontmatter, or from the classifier's safe default. A dispatch that would otherwise fall back to inheriting the session's own model must announce it as a **loud warning** and **pause for confirmation** whenever the session model is frontier-tier (T3/T4). This is the direct fix for the Mythos incident — 112 sub-agents silently inheriting `claude-fable-5` (token-audit's table names it explicitly as the recognized, unrouted, premium-priced model this exact failure defaults to), discovered only after the session limit was already exhausted.
- **Staged per-task tier hints.** A `PLAN-<slug>.md` task gains an optional per-task tier hint field, derived by the same classifier at staging time from the task's own work-type, so a staged plan carries its expected tier alongside its verification criteria without a second hand-authored judgment call.
- **Session-start tier + advisor nudge.** At session start, compare the live session's model against the active plan's tier hint (the classifier's staged output) and report a mismatch — **nudge only, it never switches** the session (the same advisory-only limit that holds wherever the frontmatter pin doesn't reach) — **and state advisor availability** (`advisorModel`, when configured), so the operator knows a stronger-model consult is one config line away without a model switch.
- **Phase-close handoff packs.** A phase-close briefing (the existing orchestration-briefing surface, e.g. at `/release`) auto-emits a model-labeled handoff pack for remaining cheap-routable work, via token-audit's `/handoff-pack` command — carrying the same machine-readable tier/model label the `/work` escalation tripwire (model-effort-routing's Escalation section) writes its entries in, one format, two producers.

**Explicit boundaries.** The main session's own model stays a human act (`/model`, `opusplan`) — this is untouched by any of the above. The **single-implementer invariant is untouched**: `/work`'s implementer never fans out (ADR 0023 / the concurrency model, below); routed dispatch only ever covers the read-only/review/docs helper agents already dispatched around it, never the implementer itself. Wiring is `enhances:` + `find_capability` probe now; it migrates to `opinion_request('efficient')` when the Wave-B opinion registry lands, consistent with this design's own already-pending hardwired→requested migration (see `### Opinions it consumes`, below). Hard prerequisite: the already-shipped R0.5 `find_capability` fallback fix (`test_find_capability.py`), which the probe path above relies on.

**As-built** — the routing table + token-audit's side of the contract (`PLAN-efficiency-automation`) and the dispatch-point wiring, the mandatory announcement, and the escalation tripwire (`PLAN-efficiency-dispatch`) both shipped and archived 2026-07-04, verified live (a Task-8 spike dispatched a real `Explore` sub-agent with `model="sonnet"` and it self-reported `claude-sonnet-5`). The routing-table read later migrated from a hardcoded relative path to `opinion_resolve('efficient')['implements']` (2026-07-06). The wiring covers each named dispatch category at representative sites behind a `token-audit` probe (graceful-skip when absent), not every literal occurrence — see the Amendment log for the honest scope note and the one open `INHERITED`-precision gap.

### Setup (one-time)

`/setup` is not a daily cycle — it runs **once per project** to lay down the substrate the cycles read.

- *Entry:* operator invokes `/setup`; the agent inventories `README`, the package manifest, `.github/workflows/`, and any existing `AGENTS.md` before asking anything.
- *Exit:* `.harness/` bootstrapped, `AGENTS.md` merged with a real `## This project` block, `init.sh` verified to boot (exit 0); operator ready for `/plan`.
- *Automated:* inventory-before-interview (ask only what files can't answer); write the scaffold without clobbering existing files; populate + verify `init.sh`; append one line to `progress.md`.
- *Enhancements:* wiki seed pages via the documenter ([wiki](crickets-wiki.md)); a GitHub backlog Project, ask-first ([github-projects](crickets-github-projects.md)).
- *Artifacts:* `PLAN.md` (seed), `progress.md`, `features.json`, `init.sh`, optional `verify.sh`, optional `project.json`; `AGENTS.md` (repo root, merged).

### Workflows

Each recurring cycle, with where it starts and stops, what it does on its own, what a sister capability adds, and where its artifacts land.

**`/plan`** — brief → task list, no code.
- *Entry:* `/plan <brief>` (`--name` / `--stage` / `--activate` modes); reads `PLAN.md` + `progress.md`, resolves the path via `resolve_plan.py`.
- *Exit:* `PLAN.md` written with a task list + per-task verification criteria (`Status: planning`); `features.json` updated only for net-new user features; ready for `/work`.
- *Automated:* resolve path + mode (never re-derive); interview into executable per-task criteria; `--activate` promotes a staged plan after a pre-flight reconcile; append one line to `progress.md`.
- *Enhancements:* design grounding — `find_governing_design.py` reads the governing design's locked calls, and a grounding gate blocks an architecture-touching plan with no cited design ([design](crickets-design.md), Hooks 2–3); pending-page declaration ([wiki](crickets-wiki.md)); board record + out-of-scope deferrals as backlog items ([github-projects](crickets-github-projects.md)); *(as-built)* the explorer fan-out routes by token-audit's routing table + emits the mandatory fan-out announcement ([token-audit](crickets-token-audit.md)).
- *Artifacts:* `PLAN.md` / `PLAN-<slug>.md` (active) or `queued-plans/PLAN-<slug>.md` (staged); `progress.md` / `progress-<slug>.md`; `features.json`; `board-items.json`.

**`/work`** — implement the tasks autonomously.
- *Entry:* `/work [--name <slug>] [task N]`; resolves the plan pair, reads the task list (first `[ ]` or `task N`), runs duplicate guards, checks isolation mode.
- *Exit:* per task — implement → **local** `check-all.sh` green → push → mark `[x]` (gated on the **previous** task's PR check-suite having gone green — pipelined, one-task lag, not this task's own checks) → log to `progress.md` → commit (one per task, no `Co-Authored-By`); the **final** task in the plan still waits synchronously for its own PR checks green before close-out (nothing left to pipeline against); on the final task, archive the plan, append a close-out, finalize the unit.
- *Automated:* per-task safety pre-check (stop mid-plan only on unrecoverable / ambiguous / scope-drifting / unverifiable / needs-clarification, else proceed); **local** gates required green before continuing to the next task; the **previous** task's PR check-suite confirmed green before the **current** task's `[x]` lands — wake-on-**PR-checks**, not wake-on-push (per-plan CI, 2026-07-06: the full matrix triggers on `pull_request`, not on push to `main`) — a red check-suite on the previous task stops the session immediately, the same trigger as any other safety-check failure, just surfaced one task later than a synchronous wait would; no tags (reserved for `/release`); autonomous close-out at plan end, which does wait synchronously on the final task's checks.
- *Enhancements:* worktree isolation + recoverability-gate finalize ([developer-safety](crickets-developer-safety.md)); `explorer` read-only fan-out for unfamiliar code *(designed: routed by the table + announced, same as `/plan`'s explorer — [token-audit](crickets-token-audit.md); the implementer itself is never routed — see the single-implementer invariant above)*; pending → implemented page flips ([wiki](crickets-wiki.md)); task-progress + backlog sync ([github-projects](crickets-github-projects.md)); a design-divergence NOTE for `/review` to adjudicate ([design](crickets-design.md), Hook 4).
- *Artifacts:* `PLAN.md` (`[ ]`→`[x]`) → `PLAN.archive.YYYYMMDD-<slug>.md` at close; `progress.md` (per-task + close-out); `board-items.json`.

**`/review`** — adversarial critique, no fixes.
- *Entry:* `/review [range / task N]`; resolves the task context (default: the most-recently-completed task), runs the gates, probes for an adversarial reviewer + a governing design.
- *Exit:* gates reported (a red gate stops the run); on green, an adversarial pass returns a failing test / `file:line` defect / explicit `NO ISSUES FOUND` + design-conformance findings, triaged into recommended `/work` follow-ups; logged to `progress.md`. It never fixes — that is `/work`.
- *Automated:* gates first, short-circuit on failure; verify each finding reproduces before reporting; group + recommend follow-ups; log the outcome.
- *Enhancements:* a deeper adversarial pass in fresh context — assume bugs exist — and a cross-model second opinion when available ([code-review](crickets-code-review.md)); a design-conformance dimension ([design](crickets-design.md), Hook 1); *(designed)* the adversarial + cross-model reviewer dispatch routes by token-audit's table + emits the mandatory fan-out announcement ([token-audit](crickets-token-audit.md)).
- *Artifacts:* `progress.md` (the outcome — `NO ISSUES FOUND` or N findings); reads `PLAN-<slug>.md` for the verification criteria.

**`/release`** — the pre-merge gate; ship.
- *Entry:* `/release`; checks preconditions (`Status: done`, all `[x]`, `/review` resolved, clean tree, branch ahead), re-runs the **full** gate suite (full tests + production build).
- *Exit:* `features.json` `passes: true` set only on verified features; version bumped + changelog written; push → wait-on-CI green → tag-reachability check → tag → release, all under the recoverability gate; plan archived; ready for the next `/plan` or `/bugfix`. *(The code itself already cleared the full-matrix PR check-suite as part of the plan's own merge to `main`, before `/release` runs — the version-bump/changelog commit's own push-triggered CI is deliberately the lighter push-to-`main` job, per-plan CI, 2026-07-06; `/release` was not moved onto a PR-check wait the way `/work` was.)*
- *Automated:* precondition gate (any miss stops); full suite (not a subset); recoverable push/tag/release announce + proceed; archive + final `progress.md` summary (SHA, version, tag, URLs).
- *Enhancements:* design reconciliation of the full diff against the locked calls — divergence blocks the release ([design](crickets-design.md), Hook 5); a documenter wiki sweep that blocks on unresolved OPEN QUESTIONS ([wiki](crickets-wiki.md)); the `ship-release` changelog + semver bump ([conventions](crickets-conventions.md)); plan + feature board closeout ([github-projects](crickets-github-projects.md)); *(designed)* the close-out briefing auto-emits a model-labeled handoff pack for remaining cheap-routable work ([token-audit](crickets-token-audit.md)'s `/handoff-pack`).
- *Artifacts:* `features.json` (`passes: true`); `PLAN.archive.YYYYMMDD-<slug>.md`; `progress.md` (release summary); `CHANGELOG.md` (repo root).

**`/launch`** — first-production-exposure readiness gate.
- *Entry:* run before a feature first reaches production users (first real-traffic flag enable, staged-flag removal, a load-shifting infra change); not for routine patches.
- *Exit:* feature launched with a rollout plan in hand — RED metrics confirmed emitting, symptom-based alerts routing to a named owner, staged-rollout stages verified, the flag's removal scheduled within the next release cycle.
- *Automated:* verify observability wiring (send a test request, confirm metrics emit); test the rollback in a non-prod environment + document time-to-recovery; test the flag off-switch (< 5 min, no code change); advance the flag lifecycle created → dark → staged → full → removed; halt a ramp if RED metrics degrade.
- *Enhancements:* the observability deep-dive (`/observe` — RED instrumentation, structured logs, trace spans) ([diagnostics](crickets-diagnostics.md)); rollback + staged-rollout safety doctrine ([developer-safety](crickets-developer-safety.md)).
- *Artifacts:* `progress.md` (launch + rollout-plan summary); the rollout plan + flag schedule (in the changelog / runbook); feature flag state lives in the app, not `.harness/`.

**`/deprecate`** — mark an interface for retirement.
- *Entry:* run when an interface with callers is being removed, migrated, or sunset; not for live interfaces with no removal intent.
- *Exit:* the interface is **marked, not removed** — classified compulsory vs advisory; callers enumerated; for advisory, a deprecation notice + per-invocation log warning + a published removal date + changelog + downstream notification.
- *Automated:* classify (compulsory = all callers in-repo; advisory = external callers exist); enumerate callers via grep + type-checker; add the canonical deprecation notice + log warning; set a published removal date (no date = not a deprecation); update the changelog.
- *Enhancements:* changelog deprecation entries ([conventions](crickets-conventions.md)); a no-living-callers check ([code-review](crickets-code-review.md)).
- *Artifacts:* `progress.md` (the deprecation + removal date); `CHANGELOG.md`; the notice + log warning live in the code being deprecated.

**`/retire`** — prove-dead-then-delete *(forward-looking — see Risks)*.
- *Entry:* run after a deprecation window (advisory) or once all callers are migrated (compulsory), to remove a previously-deprecated interface confirmed dead.
- *Exit:* the code + all conditional branches on the symbol deleted, zero remaining references, full suite green; the removal commit carries proof of the Beyoncé-rule audit.
- *Automated:* verify preconditions (notice exists / callers migrated); the Beyoncé-rule audit — grep the symbol, remove locally (no commit), run the full suite; if it fails, fix the missed caller and repeat; delete permanently; confirm an empty grep (a non-empty grep blocks the commit); changelog the removal.
- *Enhancements:* full-suite verification ([conventions](crickets-conventions.md)); a pre-commit removal-safety hook ([developer-safety](crickets-developer-safety.md)); a cross-repo reference audit for published interfaces ([code-review](crickets-code-review.md)).
- *Artifacts:* `progress.md` (the removal + audit proof); `CHANGELOG.md` (removal entry with deprecation + removal dates).

**`/bugfix`** — the alternate track *(replaces `/plan` + `/work` for defects)*.
- *Entry:* `/bugfix <report / repro>`; captures the report verbatim into `## Report`, opens a tracking issue (announce + proceed).
- *Exit:* root cause pinned (`file:line`, a 3-why chain); a regression test written first (fails without the fix, passes with it); the minimal fix; a **mandatory** `/review`; the issue closed; one line to `progress.md`. A design flaw escalates to `/plan` rather than a symptom patch.
- *Automated:* reproduce → ask why 3× → pin root cause + scope; regression-test-first; gates green; the mandatory review; issue close.
- *Enhancements:* GitHub issue threading across all four phases ([github-projects](crickets-github-projects.md)); the mandatory adversarial review ([code-review](crickets-code-review.md)); a Known-Issues page only on a durable gotcha ([wiki](crickets-wiki.md)).
- *Artifacts:* `PLAN.md` (`## Report` + `## Analysis`); `progress.md` (tracking #, root cause, regression-test path); the regression test (in the repo's test tree).

### Concurrency

The harness is single-threaded *within* a unit of coherent work and fans out *across* units. The mechanism that makes both true is **named plans + host-native git worktrees**, with integration serialized at the PR merge boundary instead of an in-repo integrator script.

- **The host owns worktree creation; the loop owns plan-binding.** `/work` calls the installed host's native worktree primitive (Claude Code: `EnterWorktree`; Antigravity: the New-Worktree-Mode / `invoke_subagent` isolated option) to get an isolated checkout, then writes the worktree-local `.harness/active-plan` marker itself — the one piece of the retired `spawn_worker.py` that has no host equivalent, because neither host has a concept of "plan."
- **Auto-spawn is authority-gated, not autonomous-by-default.** `.harness/project.json`'s `isolation.mode: worktree-per-plan` is the durable operator authority (ADR 0028) that turns this on; it is set explicitly per repo, not inferred. Absent that config, `/work` runs in direct mode exactly as it does today. An in-worktree single-owner guard still prevents nested spawns.
- **Named plans still give each unit an address.** `--name <slug>` / `--stage <slug>` / `--activate <slug>` are unchanged; `queue_status.py` remains the read-only coordinator's glance.
- **Task commits land in the worktree; a PR closes the plan.** Per-task commits (implement → gates → commit, one per task, no `Co-Authored-By`) are unchanged in shape — they just land in a host-managed worktree instead of a bespoke one. On the plan's final task, `/work` opens a PR from the worktree's branch instead of archiving-and-pushing-to-main directly; the PR carries the plan's close-out summary as its description.
- **Integration serializes at the merge boundary, not a lock file.** `integrate_worker.py`'s exclusive `integrate.lock` + merge → gate → hard-reset-on-red sequence (ADR 0023) retires outright — a PR gated by a required `check-all.sh` status check (branch protection, ADR 0029) cannot merge red, so there is no post-merge failure state to roll back from. This is a stronger guarantee than the mechanism it replaces, not a shim of it.
- **Merge is automatic on green (operator-ratified 2026-07-06).** No operator-invoked merge step replaces `/integrate-worker`'s operator-initiation carve-out — once the PR's required checks pass, it merges. See [developer-safety](crickets-developer-safety.md)'s amendment for the recoverability-doctrine framing of this call.
- **Generated-artifact single-writer (ADR 0030) is unchanged in spirit, re-homed in mechanism.** Workers still write `dist/` at the unbumped version; ADR 0029's require-branch-up-to-date-before-merge rule serializes the version-bump race the same way `integrate.lock` did. `/release` keeps owning the actual bump.
- **Orphan-worktree lifecycle (operator-ratified 2026-07-06).** A periodic sidecar (cadence: a few days) surfaces/reclaims worktrees whose branch never merged and whose session is gone; a soft doctrine reminder in `/setup`'s scaffold is the non-blocking complement. No concurrent-worktree ceiling and no bespoke spawn-announcement — both hosts' own UI already surfaces child-session/worktree creation, and a ceiling is deferred pending evidence of an actual collision.
- **Limitations.** Merge conflicts now resolve as a PR-update, not an `integrate_worker.py` abort-and-roll-back. Submodule checkouts remain shared across worktrees per-host, same caveat as before.
- **Future aspirations, narrowed.** Per-task worktrees (`isolation.mode: worktree-per-task`) and the V5-5 orchestration split are unaffected by this change and stay designed-not-built. The async board depth-maintainer (the Planner (TPM)) is designed but unbuilt, so board depth is hand-maintained until it ships.

### Opinions it consumes

The loop **requests `done`** (the check battery `/work` and `/release` run — *is it finished?*) and **`how-we-engineer`** (the phase discipline + the plan → design → architecture sizing ladder) by name. Both are **self-provider bindings** (`done` and `how-we-engineer` both `implements: crickets/development-lifecycle` — the loop *is* the standard, not a consumer of it), declared via `opinions:` frontmatter and honesty-checked by `check-opinion-self-provider-drift.py`'s `SELF_PROVIDER_BINDINGS` registry (PLAN-wave-d-personas task 3): `done` on `work.md` (the check-battery/task-marker/progress.md triad, anchored on `` marked `[x]` `` + `progress.md`) and `how-we-engineer` on `bugfix.md` (anchored on the verbatim "Report → Analyze → Fix → Verify" phrase the stub and the caller share — confirmed by grep across all six phase-command files before picking it, not assumed from the first hit). The prose itself stays hardwired in the phase-command markdown files (`work.md`, `plan.md`, `review.md`, `setup.md`, `release.md`, `bugfix.md`) — command/skill files are loaded verbatim as prompts with no runtime render step to splice a resolved opinion body into; the self-provider case never gets a marker, only the drift check, same as `good`'s `code-review`→`adversarial-reviewer.md` binding.

The routed-dispatch layer is a **third, narrower instance that IS wired** (PLAN-wave-d-opinion-wiring task 2): `escalation_tripwire.py` now discovers `tokens/scripts` via `opinion_resolve('efficient')['implements']` — `efficient.md`'s frontmatter pointer to `crickets/tokens` — rather than a hardcoded relative path. This narrow slice is real code with a real call site, unlike the two direct bindings above; the concrete tier/model/effort data still comes from `classify_work_type.py`/`routing_table.py` (the opinion's `implements:` artifact, not a duplicate — `efficient.md`'s `composes: []` means the routing lever isn't folded into the opinion body itself yet).

## Dependencies

- **The crickets development-lifecycle plugin** carries the command specs (`/setup`, `/plan`, `/work`, `/review`, `/release`, `/bugfix`, `/launch`, `/deprecate`; `/retire` is forward-looking — see Risks) + the concurrency machinery.
- **Sister capabilities** — optional enhancements, each behind a `find_capability.py` probe, graceful-skip when absent: [code-review](crickets-code-review.md) (the adversarial pass), [developer-safety](crickets-developer-safety.md) (worktree isolation + recoverability-gate finalize), [github-projects](crickets-github-projects.md) (board-sync), [design](crickets-design.md) (design grounding + conformance, Hooks 1–5), [wiki](crickets-wiki.md) (the documenter), [conventions](crickets-conventions.md) (`ship-release` + the testing gate), [diagnostics](crickets-diagnostics.md) (`/observe`, for `/launch`), *(as-built)* [token-audit](crickets-token-audit.md) (routed dispatch + the mandatory fan-out announcement + `/handoff-pack` + the versioned routing table, via a new `enhances:` edge declared on token-audit).
- **agentm is a soft dependency** — every enhancement graceful-skips when agentm (or the capability) is absent; the `.harness/` substrate is the standalone floor. [research](crickets-research.md) composes onto `/plan`'s context-gathering (the `explorer` read-only fan-out), not a cycle of its own.
- Points up at the [crickets HLD](crickets-hld.md); the requires/enhances mechanics are in [crickets-composition](crickets-composition.md); requests the `done` + `how-we-engineer` opinions by name ([agentm Opinions](https://github.com/alexherrero/agentm/wiki/agentm-opinions-and-gates)); the loop / goal launch modes are the [Personas design](https://github.com/alexherrero/agentm/wiki/agentm-personas); reads [model-effort-routing](https://github.com/alexherrero/agentm/wiki/agentm-model-effort-routing)'s tier policy, concretized through token-audit's table, for the designed routed-dispatch layer.

## Risks & open questions

- **`/retire` is forward-looking** — the lifecycle tail names it, but it is not a shipped command; its Beyoncé-rule deletion logic currently lives inside `/deprecate`. The design must not imply `/retire` is invokable today; the first build splits it out.
- **The `/deprecate` → `/retire` split must be lossless** — every step in today's deprecation logic lands in exactly one of `/deprecate` (mark) or `/retire` (remove).
- **`/observe` is a sister enhancement, not a core cycle** — `/launch` calls it; it lives in [diagnostics](crickets-diagnostics.md), linked, never folded into the loop.
- **The phase-loop depth is at altitude** — each phase's full spec lives in its command file; this design captures the lifecycle shape, the per-workflow contract, the durable-memory wiring, and concurrency, not every internal step of every phase.
- **Loop / goal modes are partly designed** — running a cycle one-off works today; the looped + goal drivers are persona launch modes, some of which are designed-not-built ([Personas](https://github.com/alexherrero/agentm/wiki/agentm-personas)).
- **`developer-safety` naming** — it kept the `developer-` prefix while this capability dropped it; `developer-safety` → `safety` is left open.
- **The routed-dispatch layer is as-built** — the table-driven dispatch, the mandatory fan-out announcement, the inheritance warning, staged per-task tier hints, the session-start nudge (tier + advisor availability), and the auto-emitted handoff pack all shipped via `PLAN-efficiency-automation` + `PLAN-efficiency-dispatch` (2026-07-04), acceptance-tested from fixture session output (the announcement line + the inheritance-warning pause) and confirmed live. Two honest residuals: the wiring covers each named dispatch category at representative sites, not every literal occurrence; and the announcement's `INHERITED` source can't yet distinguish a built-in agent's own default from true session inheritance — it stays conservative (over-warns rather than under-warns).
- **Re-audit triggers:** split `/retire` out of `/deprecate`; flip per-task worktrees + the orchestration split + the loop/goal drivers to as-built as they ship; settle `developer-safety` → `safety`; when persona activation ships, reconcile the routed-dispatch probe as the *fallback* path behind a dispatched persona's own resolved tier, rather than the primary path; migrate the routed-dispatch table read to `opinion_request('efficient')` when the Wave-B opinion registry lands; promote the session-start tier nudge to an actual switch if a host ever exposes enforceable in-session model control.

## References

- **Command specs:** crickets `src/developer-workflows/commands/` (→ `development-lifecycle`) — `setup` · `plan` · `work` · `review` · `release` · `bugfix` · `launch` · `deprecate` (+ `retire`, to author)
- **Concurrency machinery:** `scripts/` — `worktree_marker.py` · `worktree_shepherd.py` · `queue_status.py` · `task_isolation.py` · `isolation_config.py` · `preflight_reconcile.py` · `finalize_unit.py` · `resolve_plan.py` · `stage_plan.py` · `find_process_seam.py` (DC-2-compliant bridge: discovers agentm's `process_seam.py` via path-fallback, proxies `state-path {plan|progress}` calls; replaced `harness_memory.py resolve-active-plan` delegation in developer-workflows v0.25.0 / V5-4); worktree-authority + integration-lock designs in [developer-safety](crickets-developer-safety.md)
- **Durable-memory wiring:** [agentm memory-storage-seam](https://github.com/alexherrero/agentm/wiki/memory-storage-seam) (V5-6 routing-plane — identical bytes across backends; V5-3 storage cutover — device-local canonical, vault as plugin); `resolve_plan.py` seam delegation
- **Sisters:** [code-review](crickets-code-review.md) · [developer-safety](crickets-developer-safety.md) · [github-projects](crickets-github-projects.md) · [design](crickets-design.md) · [wiki](crickets-wiki.md) · [conventions](crickets-conventions.md) · [diagnostics](crickets-diagnostics.md) · [research](crickets-research.md) · [token-audit](crickets-token-audit.md) *(designed edge)*
- **Up:** [crickets HLD](crickets-hld.md) · [composition](crickets-composition.md) · [agentm Opinions](https://github.com/alexherrero/agentm/wiki/agentm-opinions-and-gates) (`done`, `how-we-engineer`) · [agentm Personas](https://github.com/alexherrero/agentm/wiki/agentm-personas) (loop / goal launch modes) · [agentm model-effort-routing](https://github.com/alexherrero/agentm/wiki/agentm-model-effort-routing) (the tier policy) · [crickets token-audit](https://github.com/alexherrero/crickets/wiki/crickets-token-audit) (the concrete routing table)

## Amendment log

**2026-07-07 — routed dispatch flipped designed→as-built (G12 AG close-out sweep).** The "Routed dispatch + mandatory fan-out announcement" section header, its `[PENDING-IMPL]` body note, the Risks bullet, and the two `*(designed)*` sister-capability markers all still framed the layer as unbuilt, but `PLAN-efficiency-automation` (routing table + token-audit's side) and `PLAN-efficiency-dispatch` (the dispatch-point wiring, the announcement, the escalation tripwire, the advisor rider, the session-start nudge, staged tier hints) both shipped and archived 2026-07-04 — the latter's Task-8 spike verified a real `Explore` sub-agent dispatched with `model="sonnet"` self-reports `claude-sonnet-5`, closing the empirical loop. The routing-table read later migrated to `opinion_resolve('efficient')` (2026-07-06, entry below). Flipped the header, the body note, the Risks bullet, and the two `*(designed)*` markers to as-built. Two honest residuals were carried into the Risks bullet rather than dropped: the wiring covers each named dispatch category at representative sites (not every literal occurrence — the shipping plan's own scope note), and the announcement's `INHERITED` source can't yet distinguish a built-in agent's own default from true session inheritance (stays conservative). *Re-audit trigger:* close the `INHERITED`-precision gap if a dispatch site gains the ability to introspect the target agent-def's frontmatter.

**2026-07-07 — the loop's direct `done`/`how-we-engineer` bindings move from undeclared hardwired prose to self-provider bindings (PLAN-wave-d-personas task 3).** `work.md` and `bugfix.md` now declare `opinions: [done]` / `opinions: [how-we-engineer]` respectively, and `check-opinion-self-provider-drift.py`'s `SELF_PROVIDER_BINDINGS` registry gained both entries — the same self-provider class already proven on `good`/`code-review`'s `adversarial-reviewer.md`. `work.md` is `done`'s canonical site (the check-battery/task-marker/progress.md triad); `bugfix.md`, not `plan.md`/`work.md` as this plan's own preamble first guessed, is `how-we-engineer`'s canonical site — confirmed by grep across all six phase-command files: "Report → Analyze → Fix → Verify" appears verbatim in the stub and in exactly one command file. No marker/interpolation for either (self-provider prose stays hardwired by design, honesty-checked only) — the "Opinions it consumes" section above and the 2026-07-06 entry below are updated to no longer claim these are undeclared or portfolio-gap examples. *Re-audit trigger:* if a future rewrite moves the check-battery/task-marker/progress.md prose or the bugfix pipeline's own description off `work.md`/`bugfix.md`, re-run the canonical-site grep before assuming the registry's caller paths still hold.

**2026-07-06 — routed-dispatch's token-audit read migrated to `opinion_resolve('efficient')`, with a same-repo fallback CI caught (PLAN-wave-d-opinion-wiring task 2).** `escalation_tripwire.py` no longer hardcodes `_HERE.parent.parent / "tokens" / "scripts"` to discover the routing table; it resolves `opinion_resolve('efficient')['implements']` (`efficient.md`'s `implements: crickets/tokens` pointer) instead — a real registry lookup replacing an ad hoc relative path. A first version of this migration made the same-repo `tokens/scripts` lookup transitively depend on agentm's opinion resolver being reachable at all — CI (no sibling agentm checkout) caught this as a real regression: `_HAS_HANDOFF_PACK` fell through to `False` and 2 tests failed on a missing `prompts.json` that pre-migration code always wrote regardless of agentm's presence. Fixed forward in the same PR: `_resolve_efficient_implements_dir()` now falls back to the same-repo conventional path (`_FALLBACK_TA_SCRIPTS`) whenever the registry is unresolvable, so a machine with no agentm sibling never regresses below the pre-migration floor; a parity test proves the registry lookup and the fallback resolve to the identical directory whenever agentm IS present, so the two paths can't silently diverge. `classify_work_type.py`/`routing_table.py` still hold the concrete tier/model/effort data (`efficient.md`'s `composes: []` means the opinion doesn't fold in the routing lever itself yet); this migration changes discovery only, not the resolved classification, proven by a fixture test asserting the announced tier/model is unchanged. `fanout_announcement.py` itself needed no change — it never imported `routing_table`/`classify_work_type` directly; only `escalation_tripwire.py` did. The loop's own direct `done`/`how-we-engineer` bindings (the two-sentence paragraph above) stay hardwired prose — command markdown has no runtime render step, the same portfolio-wide gap this plan's task 3 documents. *Re-audit trigger:* if agentm ever changes `efficient.md`'s `implements:` target away from `crickets/tokens`, the fallback constant must move with it or the parity guarantee breaks silently.

**2026-07-06 — `spawn_worker.py` / `integrate_worker.py` retired outright; the wiki's own command references land on the surviving scripts (PLAN-worktree-native-flow task 8, docs sweep).** Both scripts and the two commands that wrapped them, `/spawn-worker` and `/integrate-worker`, are deleted from `src/development-lifecycle/` and `dist/` — not migrated, not renamed. The Concurrency-machinery reference list above now names `worktree_marker.py` (writes the worktree-local `.harness/active-plan` marker once the host has already created the worktree — the LC-6 preflight-reconcile guard and the LC-2 `vault_project` divergent-override copy that `spawn_worker.py` used to carry) and `worktree_shepherd.py` (the periodic sidecar that reclaims provably-safe orphaned worktrees/branches and rebases a `BEHIND` PR via `gh pr update-branch`) in their place. *Why not keep the old mechanism as a fallback:* a PR gated by a required `check-all.sh` status check structurally cannot merge red, so `integrate_worker.py`'s merge-then-gate-then-hard-reset sequence had become redundant machinery duplicating a guarantee GitHub's own branch protection already provides more strongly — carrying both would be two serialization mechanisms for one invariant, not a safety net. This entry is a docs-only reconciliation of the 2026-07-06 entry below, which already narrated the retirement in the design body; it closes the reference-list drift that entry left behind.

**2026-07-06 — worktree-native flow: host-native worktrees + PR-per-plan replace `spawn_worker.py`/`integrate_worker.py` (operator-ratified 2026-07-06; ROADMAP-MASTER § Ratified 2026-07-06; adjudicated via the W1 verdict memo + Fable judgment pass; PLAN-worktree-native-flow).** `spawn_worker.py`'s git-worktree creation/locking machinery retires in favor of the installed host's own worktree primitive (Claude Code `EnterWorktree`; Antigravity New-Worktree-Mode/`invoke_subagent`); `/work` retains only the plan-binding marker write, since neither host has a "plan" concept. `integrate_worker.py`'s merge-then-gate-then-hard-reset (ADR 0023) retires without a replacement mechanism — a PR gated by a required status check structurally cannot merge red, so the rollback case it existed for no longer occurs. Merging is auto-on-green (no operator-invoked merge step), a deliberate one-notch loosening of the recoverability doctrine's usual posture, by explicit operator choice — armed via `gh pr merge --auto` at PR-open time, a build item in `PLAN-worktree-native-flow`, not this amendment. Worktree spawning drops its per-invocation operator-authority gate in favor of the standing `isolation.mode: worktree-per-plan` config opt-in, with an orphan-worktree cleanup sidecar (cadence: a few days, extended to stalled-PR shepherding per the Fable judgment pass) plus a soft `/setup`-scaffold reminder as the replacement safety net — no spawn-announcement (the hosts' own UI covers it) and no concurrent-worktree ceiling (deferred pending evidence). ADR 0030's single-writer defer-bump is unchanged in intent; its serialization mechanism moves from `integrate.lock` to ADR 0029's existing require-up-to-date-before-merge branch protection. Task commits move from direct-to-main to a PR opened at the plan's final task. *Why not the alternative (keep the bespoke integrator and add host worktrees only for isolation):* would run two serialization mechanisms side by side for the same invariant, and the host's own required-check gate already provides a stricter guarantee than the hand-rolled hard-reset — carrying both is duplication with no added safety. *Re-audit triggers:* the orphan-worktree sidecar is ever found insufficient (a stranded-fix recurs despite it); a concurrent-spawn collision is actually observed (revisit the deferred ceiling); either host ships a native "plan"/task-list concept that could absorb the `.harness/active-plan` marker; ADR 0029's branch-protection rules are ever weakened; GitHub's native merge queue becomes available on these repos (the Fable pass's named spike in `PLAN-worktree-native-flow`).

**2026-07-05 — `governs:` frontmatter re-pointed to `src/development-lifecycle/` (PLAN-wave-a-renames-1 task 3).** The directory this task's rename moved (`src/developer-workflows/` → `src/development-lifecycle/`); the design's coverage moves with it. No content change — the design already narrated the rename in the 2026-06-28 entry below; this closes the stale frontmatter path that entry left behind.

**2026-07-05 — Pipelined wake-on-CI cadence: one-task lag instead of synchronous per-task blocking (PLAN-ci-walltime-diet task 2).** The prior text's "gates required green before `[x]`" didn't distinguish local battery from remote CI, and in practice every task-commit paid a full synchronous wake-on-CI wait before its checkmark — measured across agentm+crickets, 2026-07-03..05: ~6.04 combined hours of CI-All wait in a single 2-day window, 90-94% of it Windows-leg execution time, against a push pattern of 4-10 tagged task-commits per plan. `/work` still pushes every task and the full matrix still runs on every push — nothing is skipped. What changes: the session doesn't block on that push's CI before starting the next task; before the *next* task's `[x]`, the *previous* task's CI is confirmed green (cheap by then — it's usually already finished in the background). A red CI on the previous task still stops the session immediately, just one task later than today.

*Why not the alternative (batch all mid-plan CI confirmation to plan-close):* the same evidence window shows real per-task Windows-only regressions landing (a worktree-list path-separator bug, a Git-Bash-resolution bug, a cross-platform dist-pollution bug) — a full-plan batching window would let several such commits stack before detection, multiplying bisection cost. A one-task lag caps detection latency at a single task while still capturing nearly all of the wall-clock win.

*Re-audit triggers:* a Windows-only regression is ever found to have survived a full one-task lag undetected (i.e., the previous task's CI genuinely hadn't finished before the next task's local implementation completed) — revisit the lag width; or wake-on-CI itself is retired in favor of some other verification model.

**2026-07-04 — P11 v2: sub-agent dispatch classifies, then routes by token-audit's versioned table, with a mandatory fan-out announcement (designed, not built).** The loop's existing dispatch points (`/plan` explorer, `/review` adversarial + cross-model reviewers, the documenter, verification-clerk-style graders) first resolve their own work-type via token-audit's `classify_work_type` classifier (persona-declared tier → role-name match → explicit `UNCLASSIFIED-DEFAULT`), then consult [token-audit](crickets-token-audit.md)'s versioned routing table at dispatch time via a new `enhances:` edge + a `find_capability` probe, passing the resolved **model** per invocation (effort stays bound via roster-wide agent-def frontmatter — the Agent tool has no per-invocation effort) — graceful degradation to today's behavior when the table/capability is absent. Adds a **mandatory fan-out announcement**, unconditional on capability presence: one line per dispatch group (role · agent count · model · tier source — table row / frontmatter / `UNCLASSIFIED-DEFAULT` / `INHERITED`), explicitly covering **built-in-agent inheritance** (Explore inherits the session model at Claude Code ≥2.1.198, so the `INHERITED` source must name that correctly once the installed CLI crosses that version) and the classifier's own miss case. **Silent session-model inheritance is forbidden** — an inheriting dispatch must announce a loud warning and pause for confirmation whenever the session model is frontier-tier (T3/T4), the direct fix for the Mythos incident (112 sub-agents silently inheriting `claude-fable-5`, the table's one recognized-but-unrouted, premium-priced model). Adds an optional per-task tier hint to staged plans (the classifier's output at staging time), a session-start nudge that reports a tier mismatch **and** states advisor availability (advisory only, never switches), and phase-close auto-emission of a machine-readable handoff pack via token-audit's `/handoff-pack`. Adds token-audit as a new sister capability (designed `enhances:` edge). Explicit boundaries recorded: the main session's own model stays a human act; the single-implementer invariant is untouched (`/work`'s implementer never fans out — routing only ever covers the read-only/review/docs helpers around it); wires via `enhances:`/probe now, migrates to `opinion_request('efficient')` at the Wave-B registry. Hard prerequisite: the already-shipped R0.5 `find_capability` fallback fix.

*Why not the alternative:* a hard block on frontier-tier inheritance (instead of warn-and-pause) would stop legitimate deliberate T4 dispatches — the Mythos failure was silence, not the choice itself, so a loud pause preserves intentional use while killing the silent case; gating the announcement on the capability being installed would re-create the exact blind spot (a bare install fans out silently) this exists to close — visibility must not depend on the table being present; routing `/work`'s own implementer through the table too would violate the single-implementer invariant (ADR 0023 / the concurrency model above) — `/work` is deliberately not a fan-out surface; letting a dispatch point guess its own work-type inline (skip the classifier) would re-open exactly the ad hoc, per-session judgment call this whole amendment exists to close — the classifier's `UNCLASSIFIED-DEFAULT` case is what makes an unmatched role visible instead of silently absorbed into whatever the dispatch site happened to assume.

*Re-audit triggers:* when persona activation ships, the table-probed read becomes the fallback path behind a dispatched persona's own resolved tier, not the primary one; when the Wave-B opinion registry lands, migrate to `opinion_request('efficient')`; promote the session-start nudge to an actual switch if a host ever exposes enforceable in-session model control.

**2026-07-04 — phase-aware-model-routing: the folded ADR 0026 re-audit trigger has fired (P14c).** ~~The host now documents `model:` + `effort:` frontmatter on skills/commands, applied for the turn the command runs (turn-scoped, not session-persistent). Consequence: the phase commands graduate from prompt-level nudge to frontmatter pin, rendered from the routing design's tier table; the nudge remains the cross-turn fallback.~~ **Corrected by P12 Task 0's live spike (same date, plan closed): command frontmatter is decorative, not enforced** — three scratch commands pinned `model: claude-opus-4-8`/`opusplan`/`claude-sonnet-5` all reported back the session's own baseline unchanged. The claim narrows to genuine sub-agent dispatch (the Agent tool's per-invocation `model` param — Task 8: `Explore` passed `model="sonnet"` self-reported `claude-sonnet-5`) plus the prose nudge, which stays the phase-command mechanism, not a fallback beneath a frontmatter pin that never existed. Why not leave it advisory: the host now applies the pin deterministically at the moment the work runs, and keeping a nudge where enforcement exists re-opens the silent-inheritance class (Mythos: 112 sub-agents inheriting Fable) — reasoning that held only for the sub-agent-dispatch case the correction narrows to, not for command frontmatter. Build lands in the P12 routed-dispatch plan (archived; see [`agentm-model-effort-routing`](https://github.com/alexherrero/agentm/wiki/agentm-model-effort-routing)'s matching 2026-07-04 correction for the full finding). *Re-audit trigger:* a future CLI version demonstrably honors command-level `model:`/`effort:` frontmatter, or ships per-invocation `effort` on the Agent tool.

**2026-06-28 — lock-down sweep (operator review).** Converted the lifecycle-flow mermaid to a house-style hand-SVG (`diagrams/crickets-development-lifecycle.svg`). Confirmed development-lifecycle is the spine every other capability builds on — the plan→…→retire loop with `/bugfix` rejoining at `/release` (renamed `developer-workflows`→`development-lifecycle`, now owning the whole arc). The folded-ADR records and the newest-first log are unchanged. Locked as a v5–v8 guidepost.

**2026-06-24 — folded ADRs 0002 / 0004 / 0023 / 0024 / 0026 / 0029 / 0030 into this design (AG Phase 4, move-and-retire).**

**0002 — `evaluator` sub-agent: read-only fresh-context grader (2026-05-13; amended 2026-05-17, 2026-05-25).** Ship `evaluator` as a standalone agent in `crickets/agents/evaluator.md` with tool allowlist `[Read, Glob, Grep]`, a PASS/NEEDS_WORK output contract, and a caller-supplied inline rubric. Coexists with `adversarial-reviewer`. Why not sidecar `rubric.md`: more machinery to maintain; inline rubric is more flexible. Why not replace `adversarial-reviewer`: the two agents serve different framings. *Re-audit triggers:* if a rubric class genuinely can't be expressed without `Bash`; if consumers want PASS/PARTIAL/FAIL; if reusable stored rubrics become needed.

**0004 — Design skill: human-facing design pipeline → agent execution handoff (2026-05-15; amended 2026-05-16).** `/design` ships as three sub-commands (`author`, `translate`, `sequence`). Stage 5 execution reuses `/work` + `/review`. One PLAN.md per part; no-Bash tool allowlist; skill lives in crickets. Why not per-stage skills: would duplicate the `/work` phase. Why not one shared PLAN.md: couples part lifecycles. Why not put `/design` in the harness: conflates customization with phase orchestration. *Re-audit triggers:* `/design author` abandoned mid-flow on first real design; part-split override rate >50%; every Claude Code release that changes phase semantics. *See also:* 0024 (the divergent command port of this skill).

**0023 — Gate the integrated tree: merge-then-gate with hard-reset rollback (2026-06-13).** `/integrate-worker` does: capture pre-merge HEAD → `git merge --no-ff worker/<slug>` → run `check-all.sh` → on red: `git reset --hard` → on conflict: `git merge --abort`. Never pushes. Why not gate-then-merge: proves the wrong artifact (validates against old base, not the main it's about to join). Why not leave main broken on failure: a broken main is a shared-state failure. `--no-ff` chosen because it records an explicit integration commit the rollback anchor and safe `git branch -d` prune both rely on. *Re-audit triggers:* `/integrate-worker` ever gains a push step or runs on a published branch; `check-all.sh` gains a step that mutates remote state.

**0024 — Package `/design` as a command, not a skill (2026-06-13; accepted at v3.10.0).** `/design` ships as a crickets command (`developer-workflows`) with three sub-verbs (`author`, `translate`, `sequence`). Deterministic work in tested Python helpers; `sequence` wires onto `stage_plan.py`. 0004 stays `accepted` — this is a divergent port, not a supersession. Why not skill packaging: crickets phase loop is uniformly commands; a skill splits the authoring step off the surface every other phase shares. Why not prompt-only: crickets leans on tested Python helpers; re-deriving harness paths in prompt prose would fork path logic. *Re-audit triggers:* sequencing ever writes or mutates the singleton `PLAN.md`; per-part naming collides with a hand-authored named plan.

**0026 — Phase-aware model routing in developer-workflows (2026-06-14).** Agent defs use `model:` frontmatter (enforced at spawn). Slash commands use a prompt-level nudge (advisory only). Routing: `worker.md`/`/work`/`/bugfix` → Opus; `researcher.md`/`tech-lead.md`/`/plan`/`/review`/`/design` → Sonnet. All overridable per-session. Why not omit the nudge: makes the routing policy invisible at point-of-use. Why not route everything to Sonnet: `/work`/`/bugfix` need the strong model; quality cliff on mis-route costs more than the premium. *Re-audit triggers:* host exposes a command-level model enforcement API; either named model is deprecated; the global `~/.claude/CLAUDE.md` token-discipline block diverges from these defaults.

**0029 — Concurrent-release coordination: tag-from-main, branch protection, single writer (2026-06-15).** Three hardening layers: (1) `check_tag_reachability.py` gate in `check-all.sh` + CI (every tag must be reachable from main); (2) main branch protection (required CI + squash/rebase-only + no force-push + linear history); (3) `/release` is the sole tag creator — `/work` and `/bugfix` explicitly prohibit tag creation. Why not pre-tag guard only: defense-in-depth catches manually-created off-main tags. Why not merge-queue bot: unjustified overhead at solo/2–4-plan scale. *Re-audit triggers:* tag scan exceeds 1 s in practice; merge-queue bottleneck becomes observable.

**0030 — Generated artifacts have a single writer: defer the version bump to the serialized integrator (2026-06-15).** Workers commit `src/` changes + regenerate `dist/` at the current (unbumped) version — never touching `group.yaml` version or `marketplace.json`. The serialized integrator on `main` owns the bump + registry + final regen. `dist-sync` gate stays fully authoritative everywhere; `version-bump` gate becomes branch-aware (advisory on `worker/` branches, authoritative on `main`). Why not per-plugin `marketplace.json` fragments: same-plugin concurrency still collides, adds fragment-assembly machinery. Why not `merge=union` driver: hides conflict by concatenating both sides, produces garbled registry entries. Why not defer regeneration too: weakens branch CI; a stale `dist/` wouldn't surface a generation break until integration. *Re-audit triggers:* concurrency grows beyond 4–5 simultaneous landers; CI topology change makes the `worker/` branch-name signal unreliable.

**2026-06-23 — added a lifecycle-flow diagram (diagram backfill).** Per the every-design-carries-a-diagram rule.

**2026-06-23 — added an Opinions-it-consumes clause (portfolio backfill).** Made explicit which opinions the loop requests by name (`done` + `how-we-engineer`) — a standard Design clause adopted across the capability designs.

**2026-06-22 — authored, reviewed, and finalized.**

The `development-lifecycle` capability — the loop a developer runs every day, plan → retirement. Formed by **merging the standalone `lifecycle` capability into the renamed spine** (`developer-workflows` → `development-lifecycle`): `/launch`, `/deprecate`, and a new `/retire` (which splits today's combined `/deprecate` into *mark* + *remove*) join the phase loop, since they are the tail of the development lifecycle, not a separate concern. The genuinely-distinct concerns still shed (`/observe` → diagnostics, design family → design, `researcher` → research; `/ci-cd` stays — it is pipeline authoring, part of the spine). The capability drops the `-workflows` suffix and the `developer-` prefix (the exception narrows to `developer-safety`).

Authored to the operator's structure and grounded against the live specs: a **bread-and-butter** Objective (no "spine"); a table Overview of the cycles first → retire; a Design of **Development Lifecycle** (four cross-cutting properties — determinism via helper scripts, gates-before-judgment, recoverable-by-default, runnable one-off/loop/goal — plus the two-layer durable-memory wiring, identical on disk with or without agentm), a separate **Setup (one-time)**, **Workflows** (each cycle's entry / exit / automated / enhancements / artifacts-with-relative-paths, sisters linked), and **Concurrency** (operator-as-coordinator-today; named plans + worktrees; fan-out-N / integrate-single-writer; limitations; future aspirations). **Built-vs-designed:** `/retire`, per-task worktrees, the orchestration split, and the loop/goal drivers are designed-not-built; full per-phase authoring is a later pass. **Re-audit triggers:** split `/retire` out of `/deprecate`; flip the designed pieces as they ship; settle `developer-safety` → `safety`.
