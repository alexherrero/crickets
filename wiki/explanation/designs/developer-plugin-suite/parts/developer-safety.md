---
title: "developer-safety: control hooks + safety conventions"
status: draft
visibility: published
author: Alex Herrero
contributors: []
created: 2026-06-03
updated: 2026-06-03
last_major_revision: 2026-06-03
prd:
project:
parent_design: ../../developer-plugin-suite.md
part_slug: developer-safety
dependencies: [enhances-schema, developer-workflows]
estimated_scope: S
---

# developer-safety: control hooks + safety conventions

## Scope

Create `src/developer-safety/` (`standalone: true`, `requires: []`, `enhances: [developer-workflows]`) — the universal control/safety layer, usable in *any* session, workflow or not:

- **Control hooks** — `kill-switch`, `steer`, `commit-on-stop` (from the `#40` `developer` seed).
- **Safety/hygiene conventions** — `commit-no-coauthor`, `worktrees-never-auto`.

Carried **AG caveat**: Antigravity plugin hooks are observe/side-effect-only, so `kill-switch`/`steer` are Claude-only-effective; `commit-on-stop` works on both. Documented, not worked around. When installed alongside `developer-workflows`, the hooks engage across the phase loop **emergently** (session-global once installed — see `auto-enable-runtime`).

## Dependencies

- **enhances-schema** — provides the `enhances:` field.
- **developer-workflows** — the enhance target (must exist for the `enhances` reference to validate).

## Verification criteria

- Plugin loads on both hosts.
- `commit-on-stop` fires (both hosts); `kill-switch`/`steer` fire on Claude Code (documented observe-only on Antigravity).
- `enhances: [developer-workflows]` validates in `lint_src.py`.
- **Works fully standalone** (no `developer-workflows` required); with workflows installed, the hooks engage across the loop.
- Generated byte-deterministically; `generate.py check` clean.
- Dogfooded on agentm / crickets / sherwood.

## Parent design

This part implements one slice of [Developer Plugin Suite](../../developer-plugin-suite.md) (`Status: final`) — Detailed Design §3 (+ the safety side of the §6 seed-split: the control hooks). See the parent for Context, Alternatives Considered, Quality Attributes, and Operations. Mid-execution scope changes must be appended to the parent's Document History.
