name: Releasing Conventions
description: Ship-release workflow and release discipline — pre-release checklist, changelog-shape convention, and paired-release discipline for cross-repo coordinated releases.
category: Coding
requires: [developer-workflows]
standalone: false
capabilities: [releasing-conventions]
# 0.1.2 = declare capabilities: [releasing-conventions] (AG Phase-2 hygiene;
#          transitional self-name — merges into `conventions` with resolver aliasing in Phase-3).
version: 0.1.2
