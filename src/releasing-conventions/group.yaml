name: Releasing Conventions
description: Ship-release workflow and release discipline — pre-release checklist, changelog-shape convention, and paired-release discipline for cross-repo coordinated releases.
category: Coding
requires: [development-lifecycle]
standalone: false
capabilities: [releasing-conventions]
# 0.1.2 = declare capabilities: [releasing-conventions] (AG Phase-2 hygiene;
#          transitional self-name — merges into `conventions` with resolver aliasing in Phase-3).
# 0.2.0 = merged agentm's mechanical ship-release executor (semver auto-sizing, CHANGELOG
#          prepend, tag/push/gh-release) into this skill; agentm's duplicate copy retired.
# 0.2.1 = add the "update the latest-release note (README + Home)" step to the mechanical
#          workflow — the merge tagged releases without it, leaving the note a version behind.
# 0.2.2 = AG Wave A rename 1: requires: [developer-workflows] -> [development-lifecycle].
version: 0.2.2
