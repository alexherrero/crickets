---
section: diagram
reusable: true
applies-to: [plugin-reference]
---
<!-- SECTION diagram — under ## Architecture. Every plugin page carries at least a
     COMPOSITION diagram (how the plugin relates to the others), and a second MECHANISM
     diagram when the plugin has a multi-step internal flow a picture clarifies. House SVG
     spec + palette + worked examples: ../style/diagram-style.md. Store each as its own
     .svg under diagrams/ and reference it with a one-line image (so it never pushes a
     table off-screen); the alt text describes the whole picture. `_None / not needed._`
     is retired for plugin pages. When both diagrams are present, put the mechanism one
     first, the composition one second. -->

### Diagram

<One-line caption ending in a colon, then the image:>

![<descriptive alt text — the whole picture in a sentence>](diagrams/<plugin>-composition.svg)
